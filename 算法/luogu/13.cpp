//P1164
#include<bits/stdc++.h>
int f[10000];
using namespace std;
int main(){
    int d, m, a[100];
    cin>>d>>m;
    for(int i=0; i<d; i++)
        cin>>a[i];

    for(int i=0; i<=m; i++)
        f[i]=0;

    f[0]=1;
    //f(i,j)+=f(i-1,j); f(i,j)+=f(i-1,j-a[i])|a[i]<=j
    //将i从空间转存到时间中,我们要仔细思考如何将空间顺序转换为时间顺序->此时i表示这一轮,i-1表示上一轮
    //1.当f(i,j)调用f(i-1,j)时,相当于这一刻的f(j)复制上一刻的f(j)的值
    //所以我们不需要做出任何行动,因为f(j)的值并没有改变
    //2.当f(i,j)调用f(i-1,j-a[i])时,我们发现f(j-a[i])是上一刻的
    //所以对f(k),f(l),k<l,f(l)可能需要上一刻的f(k)来更新自己,但f(k)却永远不需要上一刻的f(l)
    //i作为时间应该正向更新,而对于j,我们要从大到小更新
    for(int i=0; i<d; i++){
        for(int j=m; j>=1; j--){
            if(a[i]<=j)f[j]+=f[j-a[i]];
        }
    }
    cout<<f[m];
}
/*
做题思路：
- 01 组合计数：用前 i 个数凑成和 j 的方案数
- 一维滚动数组 f[j] 表示当前阶段的方案数
- 对每个数 a[i] 从大到小更新：f[j]+=f[j-a[i]]，避免重复使用当前数
- 初值 f[0]=1 表示凑出 0 的方法为不选任何数
*/
